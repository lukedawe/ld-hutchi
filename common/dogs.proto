syntax = "proto3";

package common;

option go_package = "generated/proto_dogs";

message AllDogs {
    repeated DogCategory categories = 1;
}

message DogCategory {
    string name = 1;
    repeated string breeds = 2;
}